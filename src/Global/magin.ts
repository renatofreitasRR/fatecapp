import styled from 'styled-components/native';
import { RFPercentage, RFValue }  from 'react-native-responsive-fontsize';

export const MarginLeft = styled.Text`
    margin-left: 16px;
`;

export const MarginRight = styled.Text`
    margin-right: 16px;
`;

export const MarginTop = styled.Text`
    margin-top: 16px;
`;

export const MarginBottom = styled.Text`
    margin-bottom: 16px;
`;

